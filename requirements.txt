# ITE 17 IDS Project -- Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Also required, but NOT a pip package:
#   ODBC Driver 18 for SQL Server (system-level driver, used by pyodbc)
#     - Windows: usually preinstalled, or download from Microsoft
#     - macOS:   brew install msodbcsql18   (microsoft/mssql-release tap)
#     - Linux:   follow Microsoft's apt/yum instructions for msodbcsql18

# --- Data handling ---
pandas>=2.0,<3.0
numpy>=1.24,<2.0

# --- Database connectivity (db_connection.py, data_fetch.py, import.py) ---
SQLAlchemy>=2.0,<3.0
pyodbc>=5.0,<6.0
python-dotenv>=1.0,<2.0

# --- Modeling ---
scikit-learn>=1.3,<2.0
xgboost>=2.0,<3.0
imbalanced-learn>=0.11,<1.0   # BorderlineSMOTE, SMOTEENN

# --- Persistence ---
joblib>=1.3,<2.0

# --- Streamlit App ---
plotly
streamlit